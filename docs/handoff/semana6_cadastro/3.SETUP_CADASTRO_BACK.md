# **🔐 SETUP CADASTRO: BACK-END & INTEGRAÇÃO**

Este guia é para o responsável pelo Back-End / Integração. Seu papel é injetar o Motor de Regras de Negócio (cálculo de horários), configurar as unidades dinâmicas da dosagem e plugar o salvamento de dados real no Firestore.

---

## ⚙️ Passo 1: Configuração Integral da Rede Resiliente

Precisamos garantir que o Firebase não tente conexões gRPC complexas que costumam falhar em firewalls mobile. Aqui está o arquivo de configuração mestre completo:

**Arquivo:** `services/firebaseConfig.ts`
```typescript
import { initializeApp } from "firebase/app";
// @ts-ignore (Erro falso-positivo do TypeScript com o React Native+Firebase)
import { initializeAuth, getReactNativePersistence } from "firebase/auth";
import { initializeFirestore } from "firebase/firestore";
import ReactNativeAsyncStorage from '@react-native-async-storage/async-storage';
import { Platform } from 'react-native';

// Aqui nós "puxamos" as chaves do seu arquivo secreto
const firebaseConfig = {
  apiKey: process.env.EXPO_PUBLIC_FIREBASE_API_KEY,
  authDomain: process.env.EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN,
  projectId: process.env.EXPO_PUBLIC_FIREBASE_PROJECT_ID,
  storageBucket: process.env.EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: process.env.EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
  appId: process.env.EXPO_PUBLIC_FIREBASE_APP_ID,
};

// Inicializa o Firebase
const app = initializeApp(firebaseConfig);

// Se for celular (Native), injeta o AsyncStorage. Se for Web, deixa vazio para usar o padrão do Chrome.
export const auth = initializeAuth(
  app, 
  Platform.OS !== 'web' ? { persistence: getReactNativePersistence(ReactNativeAsyncStorage) } : {}
);

export const db = initializeFirestore(app, {
  experimentalForceLongPolling: true, // 👈 Força o uso de requisições estáveis
});


/* 
Lembre-se: 
* o `export` precisa de um nome, de uma "caixa" onde o resultado daquela ação está guardado;
* `const auth = ...` é a criação da caixa (variável). 
* `getAuth(app)` é uma ação (uma função sendo executada);

Se você chamasse getAuth(app) direto em todas as telas sem guardar numa constante, o Firebase teria que "se inicializar" 
toda vez que você mudasse de tela. Isso gastaria memória, bateria do celular e poderia gerar bugs de usuário deslogando 
sozinho. 
Com o const, nós criamos o objeto uma única vez e apenas o compartilhamos com o resto do app.
*/
```

---

## 🧠 Passo 2: Injeção de Lógica de Negócios & Banco de Dados

No arquivo `app/(tabs)/add.tsx`, localize os stubs/placeholders criados pelo Front-End e injete as lógicas e integrações abaixo.

### 1. Dependências & Imports
Adicione ao topo do arquivo `add.tsx`, garantindo a presença do `Alert` (React Native) e do Firebase:
```tsx
import { Alert } from 'react-native'; // Certifique-se de importar o Alert
import { auth, db } from '@/services/firebaseConfig'; // Configuração da infra
import { collection, addDoc } from 'firebase/firestore'; // SDK do Firestore
```

### 2. O Motor de Regras de Negócio (Cálculos)
Substitua as variáveis estáticas de simulação que o Front-End deixou pelo **cálculo dinâmico em tempo de execução**:
```tsx
  // A. Mapeamento de Unidades Inteligentes (Dita como a string de dosagem se comporta)
  const unitMap: Record<string, string> = {
    "Comprimido": "mg",
    "Cápsula": "mg",
    "Gotas": "gotas",
    "Xarope": "ml",
    "Injeção": "ml",
    "Outros": "" 
  };

  // B. Motor de Geração de Horários (Quebra 24h pela quantidade de doses)
  const intervalMinutes = Math.floor((24 * 60) / timesPerDay);

  const times = Array.from({ length: timesPerDay }, (_, i) => {
    const doseTime = new Date(startTime);
    doseTime.setMinutes(doseTime.getMinutes() + (i * intervalMinutes));
    
    const hours = doseTime.getHours().toString().padStart(2, '0');
    const minutes = doseTime.getMinutes().toString().padStart(2, '0');
    
    return `${hours}:${minutes}`;
  });
```

### 3. Função de Persistência (`handleSave`)
Substitua o `handleSave` simulado pela função real conectada ao Firestore:
```tsx
  const handleSave = async () => {
    // 1. Validação de Regra de Negócio no Envio
    if (!name.trim()) {
      Alert.alert("Atenção", "Por favor, digite o nome do medicamento.");
      return;
    }

    // 2. Validação de Segurança (Identidade)
    const user = auth.currentUser; 
    if (!user){
      Alert.alert("Erro", "Usuário não autenticado. Verifique sua conexão e tente novamente.");
      return;
    }

    setLoading(true);

    try {
      // 3. addDoc(): Gravação Blindada na Coleção
      await addDoc(collection(db, "medications"), {
        user_id: user.uid, // Vínculo forte de segurança
        name: name,
        type: type,
        timesPerDay: timesPerDay,
        // Serialização inteligente concatenando valor + unidade
        dosage: dosageAmount ? `${dosageAmount} ${unitMap[type]}`.trim() : '',
        times: times, // Array computado no passo anterior
        startDate: startDate.toISOString(),
        repeatType: repeatType,
        endDate: endDate ? endDate.toISOString() : null,
        created_at: new Date().toISOString()
      });

      // 4. Fluxo de Sucesso
      setLoading(false);
      Alert.alert(
        "Sucesso 🎉", 
        `O medicamento "${name}" foi cadastrado com sucesso!`,
        [{ 
          text: "OK",
          onPress: () => {
            resetForm();
            router.replace('/(tabs)')
          }
        }]
      );
      
    } catch (error) {
      console.log("Erro ao salvar:", error);
      setLoading(false);
      Alert.alert("Erro", "Não foi possível salvar o medicamento no banco de dados.");
    }
  };
```

---

## 💾 Passo 3: Commit do Back-End & Integração
Feche o ciclo e envie as implementações de negócios e dados:
```bash
git add services/firebaseConfig.ts
git commit -m "feat(infra): configure firestore with experimentalForceLongPolling for network resilience"

git add app/\(tabs\)/add.tsx
git commit -m "feat(core): implement schedule engine and firestore data persistence in registration"
```


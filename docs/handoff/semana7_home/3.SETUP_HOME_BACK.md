# **⚙️ SETUP HOME: BACK-END (Lógica de Negócios)**

Este guia é para o Arquiteto de Software/Back-End. Seu objetivo é plugar as **3 importações de Banco e Autenticação** sobre a interface criada pelo Front-End e injetar a lógica.

---

## 🧠 Passo 1: Imports do Firebase

**Onde adicionar:** No topo do arquivo `app/(tabs)/index.tsx`, logo após as importações do React/Front-End, junte os imports cruciais de segurança e banco.

**Arquivo:** `app/(tabs)/index.tsx`
```typescript
// 1. IMPORTS DO BACK-END (Banco e Autenticação)
import { auth, db } from '@/services/firebaseConfig';
import { collection, query, where, getDocs, addDoc } from 'firebase/firestore';
import { signOut } from 'firebase/auth';
```

## 🧠 Passo 2: A Mágica do Filtro e a Autenticação

**Onde adicionar:** Dentro da função `HomeScreen`, identifique os blocos `user` e substitua a função simulada `fecthMedications` por esta implementação real:

**Arquivo:** `app/(tabs)/index.tsx`
```typescript
  // Vincula a identidade de segurança
  const user = auth.currentUser;

  async function fecthMedications(){
    if (!user) return;

    try{
      // 1. BUSCA A "RECEITA" DO USUÁRIO
      const medicationQuery = query(collection(db, "medications"), where("user_id", "==", user.uid));
      
      // 2. BUSCA O "DIÁRIO" DE HOJE (O Histórico)
      const historyQuery = query(collection(db, "history"), 
        where("user_id", "==", user.uid),
        where("date", "==", selectedDate) // 👈 Agora usa a data selecionada!
      );

      // Executa as duas buscas ao mesmo tempo para ser rápido!
      const [medSnapshot, histSnapshot] = await Promise.all([getDocs(medicationQuery), getDocs(historyQuery)]);

      // Mapeia a "Receita" E FILTRA POR DATA VALÍDA! (Correção do Bug 1)
      const list = medSnapshot.docs.map(doc => ({
        id: doc.id,
        ...doc.data()
      } as Medication)).filter(med => {
        if (selectedDate < med.startDate) return false;
        if (med.endDate && selectedDate > med.endDate) return false;
        return true;
      });

      // 3. O Segredo do Filtro: Mapa O(1)
      const historyMap: Record<string, 'taken' | 'skipped'> = {}
      histSnapshot.docs.forEach(doc => {
        const data = doc.data();
        const key = `${data.medication_id}-${data.time}`;
        historyMap[key] = data.taken ? 'taken' : 'skipped';
      });

      const expanded = list.flatMap(med => med.times.map(time => ({...med, time})));

      // 🛑 O FILTRO DO MVP: Se tem no diário (tomou ou pulou), some da tela!
      const pendingMedications = expanded.filter(med => {
        const key = `${med.id}-${med.time}`;
        return !historyMap[key]; 
      });

      pendingMedications.sort((a,b) => a.time.localeCompare(b.time));

      // 🕒 PEGANDO A HORA E DATA ATUAL (REAL) DO CELULAR
      const agora = new Date();
      const horaAtual = `${String(agora.getHours()).padStart(2, '0')}:${String(agora.getMinutes()).padStart(2, '0')}`;
      const hojeReal = agora.toISOString().split('T')[0];

      // ✂️ DIVIDINDO AS ÁGUAS (Correção do Bug 2)
      let praTomar: (Medication & { time: string, status?: string })[] = [];
      let paraDepois: (Medication & { time: string, status?: string })[] = [];

      if (selectedDate < hojeReal) {
        praTomar = pendingMedications; 
      } else if (selectedDate > hojeReal) {
        paraDepois = pendingMedications; 
      } else {
        praTomar = pendingMedications.filter(med => med.time <= horaAtual);
        paraDepois = pendingMedications.filter(med => med.time > horaAtual);
      }

      setTakeNow(praTomar);
      setUpcoming(paraDepois);
      setMedications(pendingMedications); 

    } catch(error) {
      Alert.alert("Erro", "Não foi possivelcarregar os dados. Tente novamente mais tarde");
    } finally {
      setLoading(false);
    }
  }
```

## 💾 Passo 3: Histórico, Logout e Gatilhos da Tela

**Onde adicionar:** Ainda em `app/(tabs)/index.tsx`, substitua as funções `handleTakeMedication`, adicione o verdadeiro `handleLogout` e o `useEffect` vital do React:

**Arquivo:** `app/(tabs)/index.tsx`
```typescript
  const handleTakeMedication = async (item: Medication & { time: string }, isTaken: boolean) => {
    if (!user) return;
    
    try {
      const historyLog = {
        user_id: user.uid,
        medication_id: item.id || '',
        name: item.name,
        time: item.time,
        date: selectedDate, 
        taken: isTaken,
        taken_at: new Date().toISOString()
      };
      
      await addDoc(collection(db, "history"), historyLog);
      Alert.alert("Sucesso!", `${item.name} marcado como tomado! ✅`);
      
      setTakeNow(prev => prev.filter(med => med.time !== item.time || med.id !== item.id));

    } catch(error) {
      console.error(error);
      Alert.alert("Erro", "Não foi possível registrar o medicamento.");
    }
  };

  const handleLogout = async () => {
    try {
      await signOut(auth);
      // O vigilante do _layout.tsx vai te tirar daqui automaticamente!
    } catch (error) {
      Alert.alert("Erro ao sair", "Não foi possível deslogar.");
    }
  };

  // 👈 Aqui dentro você dá a partida no motor!
  useEffect(() => {
    fecthMedications();
  }, [selectedDate]);
```

---

## 💾 Passo 4: Commit do Back-End
Conclua o ciclo salvando a integração do banco na Home:

```bash
git add app/\(tabs\)/index.tsx
git commit -m "feat(home): implement mvp history engine and taken exclusion filter"
```
